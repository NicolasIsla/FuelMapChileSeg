trainer:
  _target_: shapeft.engine.trainer.RegTrainer
  # params overwritten in run
  model: null
  train_loader: null
  optimizer: null
  lr_scheduler: null
  evaluator: null
  exp_dir: null
  device: null
  criterion: null

  # params to adapt
  n_epochs: 40
  precision: fp32
  ckpt_interval: 1000
  eval_interval: 1
  log_interval: 10
  best_metric_key: mRMSE
  use_wandb: ${use_wandb}

evaluator:
  _target_: shapeft.engine.evaluator.RegEvaluator
  val_loader: null
  exp_dir: null
  device: null
  use_wandb: ${use_wandb}
  inference_mode: sliding
  sliding_inference_batch: 8
  channel_names: ["w"]


